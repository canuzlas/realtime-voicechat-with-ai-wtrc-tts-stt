# =============================================
# FRONTEND CONFIGURATION (Vite)
# =============================================

# Backend API URL
# For development: http://localhost:3000
# For Docker: http://localhost:3000
# For production: https://your-backend-domain.com
VITE_API_URL=http://localhost:3000

# =============================================
# NOTES
# =============================================
# 
# 1. Vite Environment Variables:
#    - All Vite env variables must start with VITE_ prefix
#    - These are embedded into the build at build time
#    - Never put sensitive data here (API keys, secrets, etc.)
#
# 2. Usage in Code:
#    - Access via: import.meta.env.VITE_API_URL
#    - The app already has fallback to 'http://localhost:3000'
#
# 3. Docker Setup:
#    - When using Docker, set VITE_API_URL to match your server
#    - If server runs on port 3000, use: http://localhost:3000
#    - For same-machine Docker: http://localhost:3000
#    - For different hosts: http://your-server-ip:3000
#
# 4. Production:
#    - Set this to your production backend URL
#    - Make sure CORS is configured on the backend to allow your frontend domain
